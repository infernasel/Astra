#include <gtest/gtest.h>
#include <ir/generator.h>

// Placeholder test for IR generator
TEST(IRTest, BasicGeneration) {
    // This is a placeholder test that will always pass
    ASSERT_TRUE(true);
}